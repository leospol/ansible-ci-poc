- name: Install nginx
  package:
    name: "{{ item }}"
    state: latest
  with_items:
    - nginx

- shell: grep root /etc/nginx/sites-enabled/default

- name: Place index
  copy:
    src: files/index.html
    dest: /usr/share/nginx/html/index.html
  notify: start and enable nginx
